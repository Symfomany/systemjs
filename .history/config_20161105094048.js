System.config({
  baseURL: '/',
  defaultJSExtensions: false, // Backwards-compatibility mode sur l'extension en require()
  transpiler: "typescript" // babel, traceur, typescript (qui prends de l'ES6 natif)
});